export type Iparams = {
  id: string;
};

export type Inavlinks = {
  id: number;
  title: string;
  uniqueLink: string;
};

export type Ifooterlinks = {
  id: number;
  title: string;
  link: string;
};
